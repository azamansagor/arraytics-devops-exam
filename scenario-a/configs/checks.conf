# Exam token: root-vmi3536696-1788597274-30dab396
#
# format: name|url|expected_http_code
#
# Blank lines and lines starting with # are ignored by healthcheck.sh.
# Ports follow the shared-VPS convention in the root README.

# The Notes API running as a Swarm service (B4), published on 3111.
notes-api-swarm|http://127.0.0.1:3111/healthz|200

# Its readiness endpoint, which queries Postgres. Separate from the line above
# on purpose: if this fails while the one above passes, the app is alive and the
# database is not, and the log says so without anyone having to guess.
notes-api-ready|http://127.0.0.1:3111/readyz|200

# The placeholder service from A1, bound to loopback on 3110.
myapp-a1|http://127.0.0.1:3110/|200

   # An indented comment, to prove leading whitespace is handled.

# Deliberately wrong expected code. This line always fails, so every run has a
# red entry to compare the green ones against - and it fails for a reason other
# than the service being down, which is the distinction the report has to make.
notes-api-wrongcode|http://127.0.0.1:3111/healthz|418

# Nothing listens on 8111 yet; nginx moves there in A5. Until then this is the
# genuine "service is down" case, and curl reports 000 rather than a status.
nginx|http://127.0.0.1:8111/|200
